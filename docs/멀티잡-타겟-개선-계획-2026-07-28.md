# 멀티잡 · 공고별 타겟 개선 계획 (2026-07-28)

> 출발: 사장님 지적 **"공고 여러 개가 동시에 올라오면 공고마다 타겟군이 다를 텐데 그게 충분히 고려됐는지?"** → 확인 결과 **고려되지 않았음**.
> 3라운드 설문으로 정책을 확정하고, 3갈래 설계 조사(멀티잡 응대 · 노출 축 · 양측 UI)로 구현 순서를 잡았다.

## 확정된 정책 (설문 2026-07-28)

| 축 | 결정 |
|---|---|
| 노출 축 | **시군구(구 단위) · 희망 시간대(평일/주말×오전/오후) · 집결지 거리 반경(km)** 전부 추가 + 실무자 자유 필터 |
| 차종 축 | **불가** — `vehicle_type`이 카테고리가 아니라 모델명(레이·K5·소나타…), 값 없음 255명. "이륜 진행 안 함"이므로 **차량 유·무 축(#106 머지)으로 충분**, 세부 차종은 AI 스크리닝 ①차종에서 확인 |
| 값 없는 사람 | **'미확인'을 선택지로** — 조용한 탈락 금지 |
| AI 답변 범위 | **값이 채워진 공고만** 답, 빈 공고는 인계 |
| 공고 전환 | 전환 대신 **병행** + 에이전트가 **우선순위 변경 신호 파악** |
| 반복 질문 | 공통 답변(차종·본인 명의·시작 가능일)은 **사람 단위 1회** |
| 우선순위 | **1지망 한 자리만** 기록·표시 |
| 지원자 화면 | **맞는 자리 위 + 나머지 접기** · **카드마다 상황 배지** |
| 실무자 화면 | **한 사람 = 한 카드** + 공고 배지·탭 |
| 순서 | **전부 발사 전** (사장님 결정 — 일정 지연·미검증 코드 실전 투입 위험은 고지함) |

## 배치 순서 (위험 낮은 → 높은)

### M1 — 안전판 + 자유 필터 동선 + 시군구 축 ✅ (#107 · M1b PR)
1. ✅ **공고 결속 안전판** — `lib/agent/candidate-target.ts`로 대상 후보 판정 단일화. 지금은 공고 미선택 시 엉뚱한 공고의 AI가 꺼지고 'AI 재개'가 400으로 실패한다. 활성 후보 1개면 현재와 100% 동일 동작.
2. ✅ **파이프라인 '이 조건으로 고른 사람만 노출'** (M1b) — 버튼 문구(`이 명단에게만 노출`)·조건 요약 한 줄·원클릭(공고를 지정 노출로 전환 + 명단 include). 저장된 규칙이 있으면 '규칙 지우고 N명만' vs '규칙 두고 N명 추가' 2택(규칙 소거를 조용히 하지 않는다). **축을 늘리지 않고 자유도를 얻는 핵심 단계.**
   - 구현 중 드러난 것 3개(계획에 없던 것):
     - **전체 노출 공고에 남아 있는 예전 규칙** — 노출 방식만 전환하면 고르지 않은 '규칙 해당 인원'에게도 함께 보인다(조용한 확대). → 규칙이 있으면 2택 없이는 진행 불가(화면 disabled + 서버 400 `rule_action_required`).
     - **노출 게이팅은 후보 여부를 보지 않는다**(`isExposed`만 본다) → 전환·규칙 삭제로 이야기 중인 공고가 지원자 화면에서 사라진다. 이미 연결된 인원(관심·후보, 이탈만 제외)을 자동 include로 남긴다. 순서는 **자동 보호 insert → 명단 upsert → 전환 → 규칙 삭제**(첫 실패 시 공고가 하나도 안 바뀐 상태).
     - **선택 ≠ 조건 결과** — 조건 밖에서 직접 체크한 인원 수를 모달에 따로 표시(조건 요약이 거짓이 되지 않게).
   - 신규: `GET /api/admin/exposure/impact?job_ids=` (공고별 현황 배치 조회 — 공고마다 인재풀 전량 로드를 반복하지 않게) · `lib/exposure.ts describeRule()`(지울 규칙을 사람 말로).
   - 적대적 게이트 2회(1차 6렌즈 40건 → 25건 확인 / 재검증 7렌즈)에서 잡아 함께 고친 것:
     - **노출 축소·후보 생성 경로가 여러 개** — 파이프라인 일괄 배정 · 공고 수정 모달 저장 · 인박스 실공고 등록 · 공고 후보 추가. 보호를 `lib/exposure`(`gatherExposureProtectTargets`/`writeExposureProtectRows`/`ensureExposureIncludeForLinked`) 한 곳으로 모으고 네 경로 전부에 배선했다. **경로 하나만 닫으면 반드시 뚫린다**가 이 배치의 교훈.
     - **lost update** — 수정 모달이 열던 시점 노출 값을 무조건 PATCH에 실어, 급여만 고친 저장이 방금 좁힌 노출을 되돌리고 '되돌릴 수 없다'며 지운 규칙을 부활시켰다. 노출을 만졌을 때만 전송 + 만졌으면 서버 값 재확인.
     - **캠페인 답장 자동 편입이 거리 최근접으로 추측** — 공고가 여럿이면 안내받은 공고가 아닌 다른 라인으로 편입돼 AI가 엉뚱한 공고를 응대했다. 근거(관심 클릭 → 안내 문자 → 활성 1개)만 쓰도록 재정렬.
     - **약속·안내가 조용히 깨지는 경로 2개** — `대기자에게 안내`의 명단 밖 제외 수 미표시, 명단 밖 답장자의 무응답(AI·Slack 둘 다).

### M1b 이후로 의도적으로 넘긴 것 (게이트가 비차단으로 판정)

| 항목 | 지금 상태 | 왜 넘겼나 |
|---|---|---|
| 규칙을 **넓히는** 저장도 보호 대상이 되어 include가 생김 | 과다 보호 | 방향이 안전(사람이 공고를 잃지 않음). 정확히 하려면 '변경 후 규칙에 안 맞는 사람만' 계산해야 하는데, 조회 실패 시 저장이 막히는 새 실패 모드가 생긴다. 규칙 사용이 늘면 재검토 |
| 같은 공고 재안내(8일 후)에서 auto_linked pin이 노출 필터를 통과시켜 중복 1통 가능 | 열림 | 7일 피로도 필터가 대부분 막고, 재안내는 매니저가 의도적으로 누르는 동작 |
| `internal → external` 전환은 pull 노출이 사라지는데 경고 없음 | 열림(M1b 이전부터) | 노출 축소와 다른 축(모집 채널 변경). 경고를 넣으려면 후보 수 조회가 필요 |
| 자유 본문으로 공고를 알린 수신자는 보호 밖 | 구조적 한계 | 그 문자에 `job_id`가 없어 어느 공고를 알렸는지 알 수 없다. `대기자에게 안내`(purpose=new_job)만 보호 가능 |
| 코파일럿(draft) 모드에서는 '명단 밖 답장' Slack이 안 온다 | 열림 | 자동 편입 블록 전체가 auto 전용. 그 답장은 **미답 큐에는 보인다**(마지막 메시지가 inbound). 체크리스트에 명시 |
| 같은 사람이 반복 답장하면 같은 Slack이 반복 | 열림 | 억제 상태를 새로 저장해야 한다. 조용한 것보다 시끄러운 편이 안전 |
| `abort` 후보를 후보 추가 훅은 포함(축소 보호는 제외) | 과다 보호 | 매니저가 방금 다시 추가한 사람이라 노출을 주는 쪽이 맞다 |
3. ✅ **시군구 축** — `sido`와 동형. `matchesTextAxis(allow, v)`로 빈 값은 `'미확인'` 선택 시에만 통과. 옵션 API가 `sigungu` distinct + 시도별 그룹·건수 제공. 동명이구(중구·서구)는 **정확 일치 유지** + 시도 그룹 헤더로 구분.

### M2 — 슬롯 어휘 단일화 + 희망 시간대 축 ✅
4. ✅ `lib/admin/types.applicantAvailableSlots()` 신설 — 파이프라인 조건 바와 노출 규칙이 **한 함수**를 쓴다. 라벨도 `SLOT_LABEL` 하나로(예전엔 `평일오전`/`평일 오전`/`평일 · 오전` 3종). **`matchesSlot`은 건드리지 않았다** — 그건 비마트 슬롯 보드(지점×슬롯 확정/대기 집계, `confirmed_slot`과 짝) 전용이고 '이 사람이 언제 가능한가'와 다른 질문이다(주석으로 명시).
5. ✅ 시간대 축 추가 — 판정 순서 `available_slots`(자기 신고, 신설 컬럼) → `work_hours`(폼 토큰 → 자유 입력 파서). **`confirmed_slot`은 쓰지 않는다**(비마트 전용 개념 — 사장님 확인). 현 데이터 30명 전원이 폼 토큰도 갖고 있어 제외해도 변화 0.
   - **파서 실측(645명 전량 검증)**: 폼 토큰 250 + 파서 156 = **슬롯 확정 406명(63%)** · 미확인 239명. 미확인은 값이 `~` 한 글자(206) + 요일만·야간·새벽·범위 없는 값(33)으로, **파서로는 채울 수 없는 값**임을 데이터로 확인.
   - 파서 규칙: '평일'·'주말' 단어를 먼저 반영하고 지운다(안 지우면 '평일'의 '일'이 일요일로 읽힌다) · 요일→평일/주말(결정적) · `9:00~6:00` 같은 12시간제 종료는 +12 보정 · **12시 기준 앞뒤로 각 2시간 이상 근무하면 그 슬롯**(짧은 근무는 더 긴 쪽) · **야간(18시 이후 시작)·새벽(9시 전 종료)·시작=종료·보정 후에도 뒤집힌 범위는 미확인**(추측 금지).
     - 게이트에서 버린 두 방식: 시작 시각만 보기 → `9:00~13:00`이 오후 라인에도 걸림(27명) / 폼 창 겹침(09~14·12~17) → `13:00~18:00`이 '오전', `6:00~10:00`이 '미확인'으로 뒤집힘.
   - localStorage 저장 세그먼트 하위호환: 읽기 시 `replace(/\s/g,'')`로 레거시 라벨 흡수.
   - 판정 재료(`work_hours`·`available_slots`)를 `ExposureApplicant`에 **필수 필드**로 넣어, 배선을 빠뜨린 판정 지점이 컴파일 오류로 드러나게 했다(5곳 전부 배선 확인 — 차량·시군구 축에서 겪은 '조용한 전원 탈락' 재발 방지).
   - 게이트 재검증(유실된 18건 지적 전문을 회수해 직접 판정)에서 추가로 고친 것: **폼 재제출·매니저 수정이 오래된 AI 값에 지던 문제**(work_hours를 새로 받으면 available_slots를 비운다) · **조건 바에 '미확인' 칩 없음**(239명을 수동 명단으로 만들 수 없었다) · **'매일'·'평일'의 '일'이 주말로 읽히던 오독** · **자유 입력의 '오후' 표기를 안 읽어 `오후 1:30~5:30`이 새벽으로 뒤집히던 문제** · 미확인 칩 설명이 partial 구성을 잘못 단정 · 규칙 미리보기 숫자가 '노출 기준 전체'임을 밝히지 않은 것 · 마이그레이션 주석과 코드 불일치(빈 배열 의미).
6. ✅ 미확인 채우기 — **재확인 문자는 보내지 않는다**(맞춤링크를 넣으면 `campaign` 태깅으로 24시간 교차 가드가 발사 고지를 막는다). 대신 **AI 스크리닝이 대화 중 곁들여 확인**하고 `available_slots`로 승격한다. **필수 아님**(advance 조건에 넣지 않음 — 사장님 결정): 답이 애매하면 비우고 그냥 진행, 추측 금지, 되묻지 않음.

### M3 — 집결지 거리 반경 축 ✅
7. ✅ **거리 공식 단일화**(동작 변화 0) — `lib/geo.ts` 신설. 같은 질문에 세 답이 있었다: pool GET의 사설 `distKm`(집결지만) · announce-targets의 인라인 `min(집결지, 경유지)` · 후보 목록의 또 다른 인라인. 실측으로 공고 33(용산·한남)은 집결지↔경유지가 13.8km 떨어져 **15km 대상이 190명 ↔ 296명**, 10km는 **99명 ↔ 181명**으로 갈렸다. 네 곳을 `distanceToJobKm` 하나로 모으고 `EXPOSURE_JOB_GEO_COLUMNS`로 select를 통일했다.
8. ✅ **기준점을 공고가 정한다** — `jobs.distance_basis`('pickup' | 'nearest', 기본 `nearest`) + 수정 모달 선택 UI(사장님 결정: 공고마다 고를 수 있게). 기본값을 `nearest`로 둔 이유: '대기자에게 안내'의 15km 조건 매칭이 이미 그 기준이라, 기본을 바꾸면 기존 대상이 조용히 좁아진다(공고 33: 296→190).
9. ✅ **반경 축 판정** — `radiusKm`(1~100 정수) + `radiusIncludeUnknown`. 판정 재료(`lat`·`lng`)를 `ExposureApplicant` **필수 필드**로, 공고를 `ExposureContext`로 넘기게 해서 배선 누락이 컴파일 오류로 드러나게 했다(8곳 전부 배선 — 판정 5곳 + 관리자 미리보기 3곳).
   - 좌표 없는 186명은 **지오코딩으로 채울 수 없다**(실측: 주소가 `미지정` 106 · `미확인` 74 · `.`/`?`/`데이터없음` 6 — 전부 플레이스홀더). `주소를 몰라 거리를 못 재는 분도 포함` 체크박스로만 넣는다.
10. ✅ **쓰기 시점 가드** — 좌표 없는 공고에 반경 규칙을 저장하면 아무에게도 안 보이므로 POST/PATCH 400. **노출을 만지지 않고 집결지 주소만 지우는 저장에도 걸린다**(그때 공고가 조용히 사라진다 — 가드를 노출 블록 안에 두면 새는 경로였다).
11. ✅ 미리보기 — 공고를 함께 넘겨 그 공고 기준으로 계산하고, 기준점이 없으면 **'0명'이 아니라 '계산 불가'**로 표시(조건이 까다로운 줄 오해하지 않게) + 좌표 미확인 인원 수 표시. 수정 모달에서는 **방금 고른(아직 저장 안 한) 거리 기준**으로 계산한다 — 저장된 기준으로 재면 같은 모달에서 296명을 보고 190명을 저장하게 된다.
12. ✅ **게이트가 잡은 것(확정 13건 + 유실 30건 회수 판정)**:
   - [차단] interest·notify가 판정 재료(지원자 좌표·공고 기준점)를 select하지 않아 **반경 규칙 대상 전원이 '마감된 공고예요'로 튕겼다** — 카드는 보이는데 클릭만 400, 매니저 쪽엔 아무 신호도 없다. `as unknown as GeoJob` 캐스트가 컴파일 안전망을 무력화한 것이 원인 — select 문자열은 타입이 못 지킨다.
   - [차단] engage 캠페인 답장 게이트가 distance_basis를 안 읽어 'pickup' 기준 공고를 nearest(넓은 쪽)로 판정 — 본인 링크엔 없는 공고로 자동 편입될 수 있었다.
   - [차단] 지원자 카드 '집에서 약 N km'가 min(집결지, 경유지)로 바뀌어 통근 거리를 최대 13.8km 짧게 보여줬다 → **카드 거리는 항상 집결지(통근) 기준**으로 고정(판정 기준과 다른 질문).
   - [차단] 복제가 반경 규칙을 보이지 않게 실어 등록 가드에 걸려 **등록 자체가 막혔다**(회복 UI 없음) → 복제 시 반경은 싣지 않고 안내 토스트.
   - 공고 기준점 없음 + '주소 미확인 포함' = fail-open(전원 통과) → 기준점 없으면 포함 여부와 무관하게 차단(fail-closed). 오설정 방지는 쓰기 가드가, 방어는 판정이.
   - 전체 노출 공고에 남은 반경 규칙이 주소 수정을 400으로 막았다(규칙 편집기는 targeted에서만 열려 지울 UI가 없는 막다른 길) → 가드를 '지정 노출일 때만'으로 축소 + 파이프라인 원클릭 전환이 그 상태를 우회 생성하지 않게 전환 쪽에도 같은 가드.
   - 거리 기준·좌표 변경은 노출 필드를 안 만져도 반경 대상을 줄인다(296→190) → M1b 보호(연결 인원 pin)를 같은 조건으로 확장.
   - 지오코딩 실패 시 옛 좌표가 남아 규칙이 예전 집결지를 겨냥했다 → 주소가 바뀌었는데 좌표를 못 얻으면 좌표를 비운다(주소·좌표가 다른 곳을 가리키는 거짓 상태 금지).
   - 파이프라인 '공고 근거리순'이 네 번째 사설 공식(min 하드코딩)이었다 → lib/geo + 공고 기준으로 교체.
   - normalizeRule이 0.3km를 0으로 반올림해 '조건인 척하는 no-op' 저장 → 반올림 후 1~100 검증.

### M4 — 지원자 화면 ✅
13. ✅ **fit 판정을 서버로** — `lib/pool-fit.ts`. 노출 규칙·파이프라인과 같은 함수(normalizeVehicleOwned·applicantAvailableSlots) 재사용 — 판정 소스를 늘리지 않는다. 순서는 서버가 확정(warn을 뒤로), 클라는 그룹만 나눈다.
    - 축 2개만: **차량**(요건 있음 + 본인 '없음' 확정일 때만 warn — '미확인'은 warn 아님) · **시간대**(공고 `slot`에 4슬롯 **명시 토큰**이 있고, 본인 시간대가 판정됐고, 겹침이 0일 때만 warn — 자유 텍스트 공고는 판정하지 않음, '남는 위험 2' 그대로).
14. ✅ **맞는 자리 위 + 나머지 접기** — 카드 JSX 그대로 추출. `warn`만 "조건이 다를 수 있는 자리 N개 더 보기" 접힘, `unknown`은 위 그룹(조용한 강등 금지). **카드는 절대 숨기지 않는다.** 요건 어긋난 이유는 서버 문장을 그대로 표시(차량 전용 배너를 대체 — 판정 한 곳).
15. ✅ **게이트가 잡은 것(5렌즈 35건 → 확정 23건, 차단 6건=4갈래)**:
    - [차단] `jobSlotTokens` **반쪽 캡처** — `평일 오전~오후`·`평일/주말 오전`·`평일 오전/오후`에서 한쪽만 잡혀, **실제로 맞는 분에게 "시간대가 달라요" 거짓 배너 + 접힘**(실측 슬롯 판정자 250명 중 52~98명). 부정형 `평일오전 제외, 오후만`은 정반대 해석. → 잡은 토큰으로 설명되지 않는 요일·시간 단어가 남으면 **판정 포기(unknown)**, 부정 표기도 판정 안 함. 모듈 자기 규칙('추측 금지 — 모르면 unknown') 위반이었다.
    - [차단] **활성 카드가 전부 warn이면 화면에 카드 0장** — 헤더는 "[관심 있어요]를 눌러주세요"인데 그 버튼이 없고 빈 상태 안내도 안 뜬다(활성 공고는 있으니까). 실공고 6/6이 차량 필수라 차량 없는 165명이 정확히 이 상태. → 그 상황이면 자동 펼침 + 안내 문구.
    - [차단] **접힌 그룹이 이미 관심·이야기 중인 카드까지 삼켰다** — M1b '이야기 중인 공고가 화면에서 사라지면 안 된다'를 시각적으로 되돌림. → 접기는 `status === 'none'`(아직 손대지 않은 자리)에만.
    - [차단] **백그라운드 갱신이 카드를 그룹 간 이동시켰다** — 병합이 배열 위치는 지키는데 M4가 그룹을 `fit`·`expired`에서 파생시켜, AI 대화로 `available_slots`가 기록되는 순간 읽던 카드가 접힌 그룹으로 사라지고 아래 카드가 위로 당겨졌다(오클릭). 프로덕션 649명 전원이 `available_slots` 비어 있어 **봇이 시간대를 확인하는 첫 대화마다** 발생. → 병합 시 `fit`·`fit_reasons`를 쌍으로 동결(다음 전체 로드에서 갱신).
    - 그 외: 종료(ended) 전환이 낡은 `doneIds`에 가려 '연락드릴게요'가 부활 → 합집합에서 제외 · AUTO 모드에서 '바로 시작 가능?' 박스가 60초 안에 사라짐 → 세션 래치 · 마감된 카드에 확인 패널이 열려 인터벌이 영구 스킵 → 해제 · `0:30~0:00` 쓰레기 값이 '오전 확정'으로 새던 M2 파서 한 줄 · `job_candidates`·`notify_request` 조회 오류 묵살 → 로그(500로는 올리지 않는다).
16. ✅ **카드 상황 배지** — `lib/pool-status.ts`(금지어 주석: 확정·배정·합격·출근 금지). `talking`='이야기 중' 배지, `paused`='매니저 확인 중'(연락 약속 없음), **`ended`(종료)는 접수 상태를 풀고 '다시 관심 있어요'로** — 예전엔 종료 건에도 '연락드릴게요'가 남아 오지 않는 연락을 기다리게 했다(서버 재부상 로직과 짝). 이미 대화 중·확인 중이면 '바로 시작 가능?' 후속 질문 생략.

### M5 — 실무자 화면 ✅
12. ✅ **목록 공고 배지** — 인력풀 목록 이름 아래 한 줄을, 공고가 2건 이상이면 **공고별 칩**으로 바꿨다.
    예전 '공고지원 · 스크리닝' 한 개는 **가장 최근 공고 하나만** 말해줘서, 세 자리에 붙은 분이 한 자리만
    진행 중인 것처럼 보였다. 칩을 누르면 그 공고 기준으로 상세가 열린다(칩 2개 + `+N`, 전체는 툴팁).
13. ✅ **'사람 확인 필요' 한 사람 = 한 카드** — 사람으로 묶고 그 안에서 공고별 줄로 나눴다(같은 분에게
    전화를 세 번 하게 되던 지점). 카드 위치는 그 사람의 **가장 오래 방치된 건** 기준, 머리글 경과일도 그 값.
    탭 숫자는 '건', 카드는 '명'이라 두 숫자가 다를 때만 `N건 · M명` 한 줄을 띄워 이유를 밝힌다.
    각 줄에 **어느 공고 건인지**를 적었다 — 예전엔 지점명만 있어 공고 7개가 열리면 구분이 안 됐다.
14. ✅ **선택 상태 통합 + 공고 전환** — 파이프라인 선택을 `{applicantId, wantJobId}` 한 값으로 묶었다
    (두 state면 사람은 바뀌었는데 공고가 남아 엉뚱한 공고로 확정 모달이 열린다). 상세 패널의 '지원 공고'
    목록을 **누를 수 있게** 만들어 표시 기준 공고를 바꿀 수 있고, 바뀌면 체크리스트·확정·후속 발송이 함께
    따라간다('표시 중' 배지로 명시). 기본값은 예전과 같은 '최신'을 유지했다 — 기본이 조용히 달라지면
    확정·발송 대상이 달라진다.
15. ✅ **판정 단일화(`lib/candidate-links.ts`)** — '살아있는 결속' 정의를 한 곳으로. `agent_stage`가
    `abort`가 아닌 결속(**NULL=관심 포함**) · 시스템 공고 제외 · 실질 마감 공고 제외.
    예전 `/active-jobs`는 `agent_stage IS NULL`을 제외해서 **관심만 눌러둔 공고는 응대 화면 탭에 아예
    안 떴다** — 발사 후엔 NULL이 다수 상태가 되므로 그 제외가 곧 '안 보이는 결속'이 된다.
    기본 탭은 `defaultFocusJobId`로 **대화가 진행 중인 공고 우선**(관심이 먼저 생겼다는 이유로 빈 대화창이
    기본이 되지 않게). 탭·칩의 NULL 라벨은 'AI'가 아니라 **'관심'** — 돌지 않는 응대를 도는 것처럼
    적으면 안 된다.
16. ✅ **게이트가 잡은 것(3렌즈 31건 제기 → 16건 확인, 차단 4)** — 전부 고쳤다.
   - [차단] **인계 큐 줄을 누르면 다른 공고 대화로 조용히 옮겨졌다.** 인계 큐는 마감·시스템 공고의 paused
     건도 그대로 띄우는데(의도) `/active-jobs`는 그 둘을 제외한다 → `defaultFocusJobId`가 요청한 공고를
     버리고 다른 공고를 골랐다. 결과: 인계된 대화가 화면에서 사라지고(빈 스레드), 매니저 답장이 엉뚱한
     공고로 적재되며 `#{공고명}` 치환까지 그 공고 값으로 문자에 나간다. 탭 바도 안 떠서 알아챌 단서가 없었다.
     프로덕션 paused 2건이 정확히 그 두 종류다(시스템 더미 1 · 마감 공고 1). M5 전에는 NULL 링크가
     빠져서 `jobId=null`(전체 스레드)로 **우연히 안전**했던 것 — M5가 그 안전판을 없앴다.
     → 큐에서 지목한 공고가 살아있는 결속 밖이면 **그 링크를 탭에 끼워 넣는다**(판정은 안 건드린다 —
     매니저가 지목한 예외 탭이고 '살아있는 결속'의 정의가 아니다). 같은 사람을 이미 보고 있을 때 누르는
     경로도 같이 고쳤다(예전엔 같은 클릭이 이전 상태에 따라 다른 공고를 가리켰다).
   - [차단] **상세 패널만 판정을 안 써서 세 번째 공식이 됐다** — 상세는 종료·마감·시스템까지 세어
     '공고 2건에 붙어 있어요'라고 적는데 목록은 0건이었다(송시권·신영식이 실제 사례). 불변식이 정작
     확정이 일어나는 화면에서 깨졌다 → `isLiveLinkResolved`(서버가 계산한 마감값용 입구, 판정식은 하나)로
     `liveCands`를 만들어 배너·요약에 쓰고, 이력 목록은 계속 다 보여주되 '종료된 건'·'마감된 공고'를 표기.
     시스템 공고 제목은 `__baemin_system__` 원문 대신 '공고 미지정 (내부 처리용)'.
   - [차단] **"후속 발송도 표시 중 공고를 따라간다"가 거짓이었다** — 만남장소·첫날·앱안내는 확정 결속
     (`current_job_id`) 기준이다. 배너 문구를 사실로 고치고, 발송 박스에 **`발송 대상 공고:`** 를 이름으로
     띄우고 표시 중과 다르면 경고를 붙였다(미리보기 본문 주소로만 판별하던 상태 해소).
     덤으로 인박스 분류가 **확정인력의 결속 포인터를 덮던** 것을 막았다(`.neq("status","확정인력")` —
     `transitions.ts`와 같은 가드). 그게 바뀌면 다음 안내가 엉뚱한 라인 집결지로 조립된다.
   - **PostgREST 1000행 상한** — 649명 결속을 한 번에 조회하면 `error=null`로 **조용히 잘려** 배지가
     사라지고 '공고 없는 사람'이 된다 → 페이지 조회 + `abort`는 DB에서 먼저 제외(단 `.neq`만 쓰면 NULL이
     통째로 빠지므로 `.or("agent_stage.is.null,agent_stage.neq.abort")`).
   - **'관심' 탭에서 답장하면 화면이 거짓말을 했다** — 멈출 후보가 없는데도 '수동 응대'로 낙관 갱신돼,
     매니저는 AI가 멈춘 줄 알고 손을 떼지만 다른 공고의 AI는 계속 돌아 이중 응답이 된다 →
     서버가 **실제로 멈춘 공고 id**를 돌려주고 그것만 배지에 반영한다.
   - 그 외: 인력풀 목록이 '관심 1건'을 '순수 인재풀'로 적던 것(칩 조건 `>1`→`>0`) · 결속 0건인데 남은
     단계를 '공고지원 · 스크리닝'으로 적어 진행 중처럼 읽히던 것(→'지난 공고') · 드로어 '대화 내역' 탭이
     표시 중 공고를 안 따라가 다른 공고로 답장이 적재되던 것(표시 공고를 드로어로 승격) ·
     `N건 · M명` 줄이 필터 전 수와 비교하던 것 · 인계 줄 라벨이 시스템 공고 자리에 지원자 지점명을
     찍던 것 · 계획 문서 사실 2건(화주사 미결속은 실공고 6건 중 5건 · 복제의 노출 규칙 승계는 내부모집만).
   검증: 순수 함수 15케이스 + 두 판정 입구 일치 14케이스 통과(경계 = 마감시각 과거/미래·시스템 제목·
   제목 null·전부 관심·합성 인계 링크).
    실측 — 지금 살아있는 결속 **0건**(실공고가 전부 마감 상태라 발사 전엔 화면 변화 없음), 과거 도시락
    3개 라운드에서 **2명이 2개 공고에 동시 결속**한 이력 확인(멀티잡은 이론이 아니라 이미 났던 일).

### M6 — 멀티잡 병행 응대 (가장 무거움)

> **범위 결정(2026-08-05 사장님)**: 핵심 2개(15·16)만 발사 전에 넣고, 17~19는 실운영 반응을 본 뒤로
> 미룬다. 15·16이 없으면 다공고 발사 자체가 위험하고(모든 답장이 마지막 발송 공고로 몰린다),
> 17~19는 없어도 사람이 관제로 메꿀 수 있다는 판단.

15. ✅ **인입 라우팅 단일화** — `lib/agent/inbound-routing.ts` 신설. 세 지점이 서로 다른 기준이었다:
    웹훅=직전 outbound 앵커 / sweeper=활성 최신 1건 / draft=**단계 무관** 최신 1건(그게 종료면 다른
    공고가 활성인데도 자동 응대가 안 됐다). 같은 답장이 어느 경로로 잡히느냐에 따라 다른 공고로
    응대되던 갈림을 없앴다.
    - **앵커에서 대량·캠페인 발송 제외**(`system-bulk`·`agent-engage`) — 안 하면 공고 안내를 대량
      발송한 순간 그 문자가 '직전 outbound'가 되어 다른 공고로 대화 중인 사람의 답장까지 마지막
      발송 공고로 몰린다.
    - 판정 순서: 활성 1건 → **텍스트 명시 1건** → 대화 앵커 → 그래도 모르면 **고르지 않는다**.
    - 텍스트 명시 매칭은 **후보 공고들 사이에서 유일한 낱말만** 근거로 쓴다(`도시락`처럼 다 가진
      낱말로 고르면 절반은 틀린다). 지역 접미사 변형(`한남권`↔`한남`, `강남권`↔`강남`)을 함께
      등록한다 — 이게 없으면 두 방향으로 틀렸다(실측: "한남 쪽은요?"를 못 알아보고, `강남권`·`강남`
      공고가 함께 있을 때 접미사가 달라 한쪽에만 유일해져 **확신에 찬 오판**이 났다).
    - 판별 불가 처리: AI 자동 모드면 **24시간에 한 번만 되묻고**, 그 뒤에도(또는 코파일럿·중지 모드면
      즉시) **활성 후보 전부를 보류로 내려 매니저에게 넘긴다.** 여러 건이 보류돼도 실무자 큐는
      M5의 '한 사람 = 한 카드'로 묶여 보이므로 카드가 불어나지 않는다.
    - 되묻기 문자는 `job_id`를 **비워서** 기록한다 — 채우면 그 문자가 다음 답장의 앵커가 되어
      판별 못 한 공고를 판별한 것처럼 만든다.
    - 라우팅 단일화로 드러난 기존 구멍 2개도 함께 막았다: 판별 불가·보류 상태에서 **캠페인 답장
      자동 편입 경로로 새어 들어가던 것**(방금 "어느 자리인지 모르겠다"고 판단한 답장을 근거로
      공고를 골라 편입하게 된다).
16. ✅ **다른 공고 정보 블록 확장 + 결정적 백스톱** — 예전 블록은 공고명·단계만 실어서 다른 공고
    질문이면 **무조건 매니저 인계**였다(공고 7개면 "그 자리 시간대는요?" 같은 흔한 질문마다 인계가
    쌓인다). 이제 근무시간·근무기간·시작일·급여·집결지·차량 요건을 싣고, **없는 값은 숨기지 않고
    항목명을 '미기재'로 열거**한다 — 빈칸을 감추면 모델이 현재 공고 값으로 메운다.
    - 규칙: 적힌 값은 **그대로 인용**해 답한다(변형·환산·추론 금지) · '미기재'거나 목록에 없으면
      pause · 다른 공고 이야기를 한 턴은 현재 공고를 advance하지 않는다.
    - 결정적 백스톱(`crossJobBackstop`) — 모델이 지킨다고 믿지 않고 코드가 검증한다:
      신고한 공고가 **목록에 없으면** pause · 그 공고에 **안내할 값이 하나도 없으면** pause(지어낸
      답이다) · 다른 공고 이야기인데 **advance면 stay로 내린다**. pause면 답장 자체를 발송하지 않는다.
    검증: 순수 함수 20케이스(실제 7월 도시락 3개 공고 제목으로 재현 — 유일 낱말·공통어·2건 명시·
    지점명·접미사 변형·미기재 열거·백스톱 4갈래).
17. ✅ **게이트가 잡은 것(3렌즈 33건 제기 → 17건 확인, 차단 0 — 코파일럿 모드라 지원자에게 나가는
    문자는 없다는 판정)** — 전부 고쳤다. 굵은 것 다섯:
    - **텍스트 매칭이 앵커보다 앞서서 진행 중인 대화를 다른 공고로 튀게 했다.** 근거 낱말을 '제목의
      2~5자 낱말 아무거나'로 잡은 것이 원인 — 유일성은 **그 사람의 후보(보통 2~3건) 안에서만** 세기
      때문에 후보가 적으면 일반명사가 쉽게 '유일'해진다. 실측: 후보가 `긴급 백업 배송원 모집`과 도시락
      공고일 때 "이 모집 아직 하나요?"·"배송원 지원했는데요"가 긴급 공고로 갔다. 시간대 낱말이 더
      위험했다 — 스크리닝이 직접 묻는 말이 "평일 오전·오후, 주말 오전·오후 중 어느 때가 편하세요?"인데
      제목에 `오전`·`주말`이 들어간 라인이 있으면 **그 답이 곧 라우팅 근거**가 된다.
      → ① 자리를 가리키지 못하는 낱말 목록(모집·배송원·긴급·백업·도시락·평일·오전·주말·일당…)을 제외
      ② **앵커를 먼저 읽고, 텍스트가 앵커와 다른 공고를 가리키면 고르지 않는다**(`text_vs_anchor`).
      확신에 찬 오판 대신 되묻기·인계로 사람이 확인한다.
    - **되묻기 문자가 원리상 답할 수 없는 답을 요구했다.** "지역 이름으로 답장해 주세요"라고 했는데
      `강남권`·`강남·신사` 공고가 함께 열리면 '강남'은 유일 낱말이 아니라 매칭이 안 된다 → 지원자가
      지시대로 답해도 다시 판별 불가 → 이미 되물은 뒤라 **전 공고가 보류**로 떨어진다. 발사 라인의
      중심 지역이 강남이라 해당 인원이 가장 많다. → 문구를 **매처와 같은 계산**으로 생성해
      `· 강남권 기업 도시락 배송 → '강남권'`처럼 낱말을 지정하고, 낱말을 만들 수 없는 조합(제목이
      같고 지점만 다른 경우)이면 **되묻지 않고 바로 인계**한다.
    - **보류가 직전 단계를 남기지 않아 'AI 재개'가 exploration으로 되돌렸다** — 온보딩·근무 중이던
      사람이 스크리닝 처음으로 리셋되고 안내 문자가 다시 나간다. → `transitions.ts`와 같은 규약으로
      행마다 `meta.paused_from_stage`·`pause`를 남긴다.
    - **되묻기가 수신거부 분류보다 먼저 나갔다** — "그만 보내세요" 답장에 "어느 자리 말씀이세요?"가
      발송된다. → 웹훅은 처리를 가용성 분류 뒤로 옮기고, sweeper는 같은 분류 함수를 호출해 넘긴다.
      **전역 '중지(off)'에서도 stage를 바꾸던 킬스위치 우회**도 함께 막았다(off면 아무것도 하지 않는다).
    - **코파일럿에서 백스톱 pause가 걸리면 아무 흔적이 안 남았다** — `reply_text`를 비워서 초안·보류·
      Slack이 전부 없고, sweeper가 같은 문자를 10분마다 재처리했다. → 비우지 않는다(발송 차단은
      `skipReplyDueToPause`가 담당). 초안에 백스톱 사유를 `need_info`로 남기고 헤더에 `[다른 공고 응대: #id]`.
    - 그 외: 백스톱이 사실상 공고 id만 검증했다(`vehicle_required`가 NOT NULL DEFAULT true라 '값 없는
      공고' 분기가 죽은 코드였고, 물어본 항목이 미기재여도 통과) → **항목 단위 자기신고**
      `answered_other_job_fields`를 받아 미기재 항목을 답했다고 하면 pause · 다른 공고 상세 집결지
      주소가 '인용해도 된다'와 함께 실렸다 → 지원자 카드와 **같은 함수**(`lib/geo.coarseArea`)로
      '서울 서초구'까지만 · 마감 공고가 블록에 실려 충원된 자리를 안내할 수 있었다 → 매니저 화면과
      같은 판정(`isLiveLink`)으로 제외 · advance를 stay로 내리면 그 턴이 완전 침묵(문자·초안·인계 전무)
      → pause로 · sweeper가 초안·재실행 검사보다 먼저 보류시켰다 → 순서 교정 · 시스템 더미 공고를
      pool에서 빼면서 일반라인 대화가 실공고로 재라우팅됐다 → 앵커는 시스템 공고 후보까지 포함해 찾는다.
    재검증: 순수 함수 케이스를 게이트 재현 입력으로 확장(일반명사 5·시간대 낱말 2·접미사 4·되묻기
    문구↔매처 일치 3·동일 제목·집결지 대략화·백스톱 4) 전부 통과.
17. ⏸ **공통 답변 사람 단위 승격**(발사 후) — 값은 기존 컬럼 재사용(`own_vehicle`·`vehicle_type`·`self_ownership`·`available_date`), 신설은 확인 메타 `applicants.profile_confirmed JSONB`뿐. **긍정 방향만 승격**('있음→없음' 강등은 pause).
18. ⏸ **`current_job_id` 잠금 해제**(발사 후) — 발송 중복 가드를 다른 기준으로 교체하고 포인터는 '확정 결속'으로만 축소. **같은 배포에 확정 대상 공고 필수화**(서버 400)를 넣어야 확정 후속(만남장소·첫날)이 대상을 잃지 않는다.
19. ⏸ **1지망 한 자리**(발사 후) 컬럼 + 매니저 수동 변경 + 양측 배지 → 20. 에이전트 신호 감지로 자동 갱신 → 21. 잔여 포인터 데이터 위생.

<details><summary>원래 계획 문구(15·16)</summary>

15. **인입 라우팅 단일화** — 지금 3곳이 서로 다른 기준(웹훅=직전 outbound 앵커 / sweeper·draft=최신 후보). `lib/agent/inbound-routing.ts`로 통일하고 앵커에서 **캠페인 발송을 제외**한다(그게 없으면 7개 동시 발사 시 모든 답장이 '마지막 발송 공고'로 몰린다). 텍스트 명시 매칭은 **정확히 1건일 때만** 채택, 2건 이상은 되묻기(24h 내 2회면 pause).
16. **다른 공고 정보 블록 확장 + 병행 규칙** — 블록에 답할 수 있는 값을 싣고 **빈 항목을 항목명으로 열거**한다(빈칸을 숨기면 모델이 현재 공고 값으로 메운다). 결정적 백스톱: tool에 `answered_other_job_id`를 자기신고받아 그 공고가 비어 있으면 transition을 pause로 강등.

</details>

## M6 이후 후보 — '상시모집'(사장님 메모의 '캠페인') 검토 결과 (2026-08-04)

> 메모 요지: 성격이 비슷한 배송라인(위펀처럼 증차·변경이 잦은 화주사)은 라인마다 공고를 만들지 말고,
> 더 넓은 대상에게 "이런 성격의 라인을 상시 구인한다"를 알려 관심자를 미리 모아두고, 실제 투입이
> 필요할 때 에이전트·매니저가 컨택한다. 공고는 그보다 좁고 급한 1~2개 라인용.

**결론: 방향 재고는 필요 없다. M1~M6를 그대로 끝내고 그 위에 얹는다.** 근거 네 가지.

1. **이미 채택한 2층 모델이고 라벨만 반대다.** `PRODUCT_DIRECTION.md` §6.3·§6.4-2가 이미
   "수명 몇 주짜리 모집 그릇 / 수명 몇 시간~일짜리 투입 요청"으로 나눠 놨다. 거기서는 긴 쪽을 **공고**,
   짧은 쪽을 **요청(shift_request)**이라 부르고, 메모는 긴 쪽을 **캠페인**, 짧은 쪽을 **공고**라 부른다.
   구조는 같다 → 새 설계가 아니라 **명명 결정**이다.
2. **'캠페인'은 이 시스템에서 이미 다른 뜻으로 돈다** — 재컨택 문자 대량 발송(캠페인 퍼널·캠페인 통계
   화면, `campaign` 발송 태그, 24시간 교차 가드). 같은 화면에 두 뜻이 있으면 "캠페인 관심자"와
   "캠페인 발송 대상"이 구분되지 않는다 → **'상시모집'·'라인풀' 같은 다른 이름을 쓴다.**
3. **비용은 새 시스템이 아니라 `jobs` 컬럼 2개 수준.** 상시모집이 필요한 배관은 전부 공고 id에 이미 붙어
   있다(노출 규칙·명단 M1~M3 · 지원자 카드 M4 · `job_candidates`는 `job_id` NOT NULL + 유니크 ·
   문자 이력 · 에이전트 컨텍스트). 필요한 건 `kind`(공고/상시모집)와 `parent_job_id`(상시모집→열린 라인)
   정도이고, 관심자를 새 라인의 노출 명단으로 옮기는 것은 **M1b의 원클릭 그대로**다.
   선행 위생 하나: 실공고 6건 중 5건이 `client_id`가 비어 있어(연결된 건 비마트 공고 id=2 하나뿐)
   화주사 단위로 묶을 근거가 사실상 없다.
4. **순서를 뒤집으면 위험하다.** 상시모집으로 들어온 사람에게 "이 상시모집에서 지금 열린 라인은 A·B"를
   안내하는 것이 곧 **M6의 '다른 공고 정보 블록'**이고, 한 사람이 상시모집 1 + 공고 2와 동시에 대화하는
   것이 **M6 병행 응대**, 큐에서 그 사람이 카드 3장으로 불어나는 것이 **M5**다. M6 없이 상시모집을 열면
   "모든 답장이 마지막 발송 공고로 몰리는" 상태에서 관심자를 대량으로 모으는 셈이 된다.

**메모가 지목한 노동의 실제 위치** — "8라인=8공고 각각 노출대상 만들기"는 M1~M3 + 복제로 이미 상당히
줄었다 — 단 **내부모집 공고만** 복제가 노출 규칙까지 함께 복제한다(반경 제외: 집결지 좌표가 저장 후에
잡히므로). '새로 모집(external)'은 노출 설정 전체가 리셋되고, M1b 수동 지정 명단은 어떤 경우에도
복제되지 않는다 — 둘 다 복제할 때 토스트로 알리고 등록 후 다시 지정해야 한다.
남는 진짜 문제는 ① 지원자가 카드 8장 중 고르기 어렵다 ② **라인이 열리기 전에 관심을 담을 그릇이 없다**
(상시모집의 유일한 진짜 신규 기여) ③ 한 사람에게 공고 수만큼 문자(=M6 라우팅, 이미 최대 위험으로 등록).

**지금 공짜로 넣어두는 안전판** — 확정 대상 판정을 한 함수로 유지하고(상시모집 제외가 한 줄이 되게),
"모든 공고는 확정 대상"을 코드에 못 박지 않는다. 둘 다 이미 불변식이라 추가 작업은 없다.

## 반드시 지킬 불변식

- **판정은 한 곳에만** — 차량(`normalizeVehicleOwned`)·슬롯(`applicantAvailableSlots`)·대상 후보(`resolveCandidateTarget`)·인입 라우팅(`pickCandidateForInbound`). 이 코드베이스는 "같은 개념 두 공식"으로 두 번 사고를 냈다.
- **fail-closed 방향** — 판정 재료가 없으면 노출하지 않는다. 단 '미확인'을 매니저가 명시적으로 고를 수 있게 해서 조용한 탈락은 없앤다.
- **fit ≠ 노출** — 맞지 않는 자리도 보이게 두고 순서·접기로만 다룬다(차량이 새로 생긴 분을 막지 않는다).
- **확정 뉘앙스 금지** — 상황 배지·병행 응대 문구 어디에도 배정·확정 암시를 넣지 않는다.
- **보조 집계 금지** — 충원율에 `confirmed_at` 같은 두 번째 기준을 추가하지 않는다(이미 두 번 겪은 오탐 원인). 신규 확정부터 포인터가 반드시 채워지게 하는 방향으로만 고친다.

## 남는 위험

1. **M6는 실전 첫 운영에서 검증되지 않은 코드가 돈다** — 킬스위치를 첫날 `코파일럿(초안만)`으로 두고 관찰하는 것을 전제로 한다.
2. `jobs.slot`이 자유 텍스트 80자라 공고 쪽 시간대 판정은 오탐 여지가 있다 → 명시 토큰만 판정, 그 외 `unknown`.
3. 좌표 미확인 186명은 반경 규칙에서 항상 빠진다(또는 '미확인 포함'을 켜야 들어온다) → 지오코딩 백필이 선행 과제.
