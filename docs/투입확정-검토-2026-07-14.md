# 투입 확정 프로세스 전반 검토 — 2026-07-14

송시권(#32 도시락 월~토 라인) 투입 확정을 앞두고 확정·온보딩 플로 전반을 검토(워크플로 41 에이전트, 33개 갭 적대적 검증). 근본 원인: **확정·온보딩 플로가 비마트(배민커넥트) 시대에 만들어져 도시락 등 internal 정기 라인을 상정하지 않음.** 확정 단계가 파일럿의 마지막 미전환 구간.

## 판정
조건부 가능이었음 → **PR #65로 블로커 2 + 안전 2 해소**. 이제 송시권 #32 확정은 표준 UI로 안전하게 가능(수동 우회 불필요).

## 처리 완료 (PR #65)
- [블로커①] 확정 대기 큐(confirm/pending) 배민 전제 게이트 제거 → 라인 형태 무관(스크리닝 완료 + 비마감 공고 링크). 도시락 라인도 큐에 뜸.
- [블로커②] 충원율 이중 계상 → 마감·abort 링크 제외. 진행 공고에만 확정 반영.
- [안전①] 라우터가 '확정인력'이면 AI 자동응대 정지.
- [안전②] webhook campaign-reply 편입에서 '확정인력' 차단.

## 남은 개선 (should-fix) — 미착수, 우선순위순
1. **확정 커밋을 공고에 결속 + current_job_id 이관** — 확정 시 실투입 job_id를 함께 저장, ALLOWED_FIELDS에 current_job_id 추가(열린 공고 & 본인 job_candidates에 존재하는 값만). 지금은 확정이 person-level이라 "어느 공고 확정"이 데이터에 없음. line_experience 오태깅(stale current_job_id 따라감)도 이걸로 해결.
2. **첫날규칙·만남장소 문안 라인 형태 분기** — buildFirstDayRules/buildVenueGuideText가 배민 배차 모델("배차 수락", "60분 내", "카카오채널 사진")을 하드코딩. isGeneralLineJob(internal)이면 정기배송 라인용 문안으로 분기. 최소안: 발송 미리보기 div를 editable textarea로.
3. **만남장소 집합 시각 하드코딩(07:50) 제거** — buildVenueGuideText에 meeting_time 파라미터 + 발송 모달 시각 입력.
4. **확정 모달 원스톱화** — start_date(기본=공고 start_date)·확정 지점을 모달에서 함께 받아 commitConfirm이 함께 PATCH. 지금은 start_date=null로 확정 가능.
5. **확정 시 지원자 자동 통보 경로** — 확정 모달에 만남장소/시작 안내 발송 액션 연결(job_id 기준 confirm/send). 도시락 문구 분기와 병행.
6. **#33 잔존 후보 자동 정리** — 확정 시 대상 공고 외 진행/보류 job_candidates를 abort+closed_at 처리(1의 current_job_id 이관과 함께).
7. **LiveConsole 컨텍스트가 닫힌 #33으로 잡힘** — active-jobs route에서 closed 공고 후보 제외.

## 사소 (nice-to-have)
- line_experience 하류 소비처 0개(오늘 무해) — 매칭/추천/pull 도입 전 태깅 근거를 실투입 공고로 고정.
- confirmed_branch 자동채움 '미지정' 방지(branch1='미지정'이면 null 유지).
- 확정 슬롯 4분류(평일/주말×오전/오후)가 도시락 월~토 종일과 불일치 → 슬롯 보드 확정 카운트 0(충원율 무관). 슬롯 모델 확장 검토.
- 선탑 완료(suntop_done)는 확정과 코드상 분리(의도된 설계). 확정 모달에 '이미 선탑 완료' 옵셔널 프롬프트로 깜빡 방지 가능(자동기록 금지 — 허위 원장 방지).

전체 원본 리포트: 워크플로 wf_bae639a4-078 결과(세션 tasks/wtorfvaga.output).
