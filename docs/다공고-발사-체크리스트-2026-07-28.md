# 다공고 동시 게시 발사 체크리스트 (2026-07-28)

> 공고 6~7개를 한 번에 올려 **내부 인력풀('우리 인력에게')** 로 구인하는 첫 운영. 이 문서는 매니저가 그대로 따라 하는 순서다.
> 근거: 5갈래 코드 감사(등록·지원자화면·발송·AI응대·관제) + 프로덕션 데이터 실측. 코드로 막은 것은 `[코드]`, 사람이 지켜야 하는 것은 `[사람]`.

## 지금 인력풀 실측 (2026-07-27 기준)

| 항목 | 수 |
|---|---|
| 인력풀 전체 | 645명 (전원 맞춤 공고 링크 보유) |
| **문자 도달 가능** | **565명** (링크·연락처 있고 수신거부·부적합·이탈 아님) |
| 신선한 인력풀(스크리닝 전·대기자) 중 도달 가능 | 461명 |
| 이미 확정된 인력 | 54명 (비마트 중단으로 대기 상태 — 안내 대상에 넣을지 매번 판단) |
| 연락처 없는 인력 | 5명 (그중 **확정인력 1명(안현진)** — 연락처 확인 필요) |
| 전화번호 중복 | 2쌍 (`[코드]` 같은 번호는 1통만 나가게 막았음) |

발송 비용 감각: 565명 × LMS ≈ **2만원 내외**(1통 기준).

---

## 1. 공고 하나를 만들 때 반드시 채우는 값 `[사람]`

7개 전부 아래를 채운다. **비우면 AI가 그 질문마다 매니저에게 넘긴다**(7개면 인계 큐가 7배가 된다).

- [ ] **제목** — 7개가 서로 다르게. 권역·요일·시간대를 넣는다(예: `강남·신사권 오전`). 목록에서 공고를 구별하는 축은 사실상 제목뿐이다.
- [ ] **화주사** — 고른다. 비우면 목록의 화주사 필터에서 그 공고가 사라진다.
- [ ] **모집 인원** — 실제 티오를 타이핑한다. **기본값 1이 가장 위험**하다: 1명 확정되는 순간부터 나머지 관심자에게 "자리가 모두 차 있어요"가 나간다.
- [ ] **마감시각** — 넣는다. 비우면 영구 게시 + 마감 안내 모드가 작동하지 않는다. 정기 라인이라 일부러 비울 때는 **모집 기간을 '정기'로** 반드시 선택.
- [ ] **시작일** — 오늘 이후. (`[코드]` 지난 날짜는 등록이 차단되고, 복제할 때는 비워진 상태로 들어온다.)
- [ ] **급여** — 단가 형태 + 금액 + 정산 문장. **라인마다 다르면 복제로 따라온 값을 반드시 지우고 새로 쓴다.**
- [ ] **집결지 · 마지막 경유지** — 주소를 넣고 저장 후 좌표가 잡혔는지 확인. 비우면 거리 정렬·거리 표시·만남장소 문안이 전부 죽는다.
- [ ] **근무시간** — '우리 인력에게'는 자유 텍스트다. 비우면 지원자 화면에서 근무시간 줄이 사라지고 시간 문의가 전부 인계로 온다.
- [ ] **AI 응대 근거** — `근무 상세 · AI 응대 근거` 접이식을 **열어서** 확인한다(기본 접혀 있다). 라인마다 다른 단가만 각 공고에 넣고, 7개 공통 정책(정산일·유류비·선탑·법인차)은 **에이전트 두뇌 탭 지식에 한 번만** 넣는다.
- [ ] **모집 방식 = 우리 인력에게** (기본값). '새로 모집'이면 맞춤 공고 링크에 안 뜬다.
- [ ] **노출 대상 = 전체 노출**. 지정 노출을 쓸 거면 조건을 최소 1개 넣는다(비우면 아무에게도 안 보이는 공고가 등록된다).
- [ ] 초안 영역에 **`오프라인 템플릿` 회색 배지**가 보이면 제목·본문을 손본다(AI 실패 폴백이라 제목이 `[] 배송 모집`으로 나갈 수 있다).

## 1-2. 공고마다 타겟군이 다를 때 `[사람]`

7개가 권역·차량·시간대가 다른 라인이라면 **"전체 노출"로 두면 한 사람에게 7장이 다 보입니다.** 지금 쓸 수 있는 타겟 수단은 아래 세 가지뿐이고, 각각 한계가 있습니다.

| 수단 | 어디서 | 쓸 수 있는 조건 | 한계 |
|---|---|---|---|
| **자동 노출 규칙** | 등록·수정 모달 `노출 대상` → 지정 노출 | 지역(시·도) · **시군구(구 단위)** · **차량** · **희망 시간대(평일/주말 × 오전/오후)** · 가용성 · 선탑 완료 · 최근 지원 N개월 · 각 축의 `미확인` | 가용성은 값이 6명뿐이라 켜면 사실상 0명. 집결지 반경은 아직 없음(M3) |
| **수동 명단(권역 타겟팅)** | 파이프라인 → 조건 바로 좁히기(+`거리 기준 공고`·정렬 `공고 근거리순`·`상위 N명 선택`) → **`이 명단에게만 노출`** | **조건 바의 모든 조건**으로 고른 아무 집합. 공고를 `지정 노출`로 전환까지 한 번에 | 공고를 저장한 뒤에만 가능(등록 시점엔 명단 UI가 없음). 거리 정렬은 좌표 있는 459명 대상 |
| 발송 대상 좁히기 | 파이프라인 조건 바 | 지역·차량·발송 준비 조건 | 노출이 아니라 **문자 대상**만 좁힘 |

**권장 순서 (7개가 권역별 라인일 때)**
1. 7개를 **전체 노출**로 등록한다(규칙을 비운 채 '지정 노출'로 저장하면 아무에게도 안 보인다).
2. 파이프라인에서 그 공고에 맞게 **조건 바로 좁힌다** → 인원 선택 → **`이 명단에게만 노출`** → 공고 하나 체크 → `전체 노출 공고를 '지정 노출'로 함께 전환`을 켠 채 적용. **한 번에 전환 + 명단 저장**이 끝난다.
   - 모달 상단에 **지금 걸린 조건이 한 줄로** 뜬다. 나중에 "왜 이 사람들인지" 되짚는 유일한 기록이니 적용 전에 읽는다.
   - 조건 밖에서 직접 체크한 인원이 섞여 있으면 그 수를 따로 알려준다.
3. 차량·시군구 같은 축을 **자동으로** 걸려면 수정 모달의 `노출 대상` 규칙을 쓴다. 두 방식은 **함께 적용**된다(규칙 매칭 ∪ 수동 추가, 수동 제외가 최우선).
4. 규칙이 이미 있는 공고에 명단을 추가하면 **2택**이 뜬다 — `규칙 두고 추가`(규칙 해당 인원 + 명단) / `규칙 지우고 이 명단만`. **지운 규칙은 되돌릴 수 없다**(확인 창이 지울 규칙의 조건과 해당 인원 수를 보여준다).
5. `[코드]` **이미 그 공고로 연결된 분**(관심 클릭·후보 + **그 공고 안내 문자를 받은 분**)은 노출을 잃지 않는다 — 자동으로 명단에 남는다. 파이프라인·수정 모달 **어느 경로로 좁혀도** 같고, 전환 뒤에 새로 추가한 후보도 명단에 함께 들어간다. 이야기 중인 공고가 본인 화면에서 사라지면 AI만 그 공고를 말하는 상태가 되기 때문이다. (직접 `제외`해둔 분은 그대로 제외.)
6. `[사람]` **공고 수정 모달과 파이프라인을 동시에 열어두지 않는다.** 수정 모달은 열던 순간의 노출 값을 들고 있다 — `[코드]` 노출을 만지지 않은 저장은 이제 노출을 덮어쓰지 않고, 만졌으면 그 사이 바뀐 사실을 알려 덮어쓸지 묻는다. 그래도 헷갈리지 않게 한 쪽을 닫고 작업하는 편이 안전하다.
7. `[사람]` **'새로 모집'(external) 공고는 지정 노출로 바꿀 수 없다** — 맞춤 공고 링크에 애초에 뜨지 않는 공고다. 모달에 `새로 모집 — 링크 미노출` 배지로 표시되고 전환은 서버가 막는다.

> ⚠️ **지정 노출로 바꾼 공고는 명단 밖 사람의 링크에서 사라진다.** 명단 밖 수신자는 문자를 받고 링크를 열어도 "지금은 모집 중인 공고가 없어요"만 본다. 좌표 없는 분이 186명이라 거리순 명단으로는 이분들이 어떤 명단에도 안 들어간다.
> **그래서 7개를 전부 지정 노출로 좁히면 §3의 '단 1통' 고지를 그대로 쓸 수 없다.** 둘 중 하나를 고른다:
> - **(A) 명단별로 나눠 보낸다** — 명단마다 조건 바를 같게 맞추고 각각 1통. 두 명단에 겹친 분은 24시간 교차 가드가 막아 첫 통만 받고, 그 분 링크에는 본인 대상 공고가 다 뜬다.
> - **(B) 최소 1개는 전체 노출로 남긴다** — 그러면 아무도 빈 화면을 보지 않는다. 고지 1통 유지.
>
> `[코드]` 명단 밖인 사람이 답장하면 AI 자동 편입은 하지 않지만 **Slack으로 알린다**(`열린 공고 N개가 모두 이분의 노출 대상 밖`). 예전엔 그 답장이 AI·Slack 양쪽에서 조용히 사라졌다.
> `[사람]` 단 **첫날처럼 `코파일럿(초안만)` 모드일 때는 이 Slack이 오지 않는다** — 그 답장은 실시간 응대 `내가 답할 차례` 큐에만 뜬다. 코파일럿 기간에는 그 큐를 매일 직접 훑는다.

**희망 시간대로 고를 때 알아둘 것** `[사람]`
- 실측: **슬롯 확정 408명 · 미확인 237명.** 미확인은 지원 폼에 시간대를 안 남기신 분(값이 `~` 한 글자)과 야간·새벽 근무 희망자다. 이분들은 **`미확인` 칩을 함께 골라야** 규칙에 들어온다(칩 아래에 인원과 이유가 표시된다).
- `평일오전 279명 · 평일오후 268명 · 주말 156명` — 오전·오후 라인이면 이 축만으로도 상당히 갈린다.
- `[코드]` 자유 입력(`월,화,수,목,금 9:00~18:00` 형태 171명)도 자동 해석된다. 단 **야간·새벽은 일부러 미확인으로 남긴다**(4슬롯 체계 밖이라 추측하지 않는다).
- `[코드]` AI가 스크리닝 대화 중 시간대를 곁들여 확인하면 그 값이 폼 값보다 우선한다. **필수가 아니라** 못 받아도 진행에 지장 없다.

**지원자 쪽에서 지금 보이는 것**: 카드에 `차량` 요건과 `집에서 약 N km`가 뜨고, **차량이 필요한 자리인데 등록된 정보에 차량이 없으면** 그 사실을 카드와 확인 단계에서 알려줍니다(자리를 감추지는 않습니다 — 차량이 새로 생겼을 수 있으니 판단은 지원자가 합니다).

## 2. 7개를 만드는 순서 `[사람]`

1. **1번 공고는 AI 초안으로 등록**(초안 없이는 등록 버튼이 잠긴다).
2. 2~7번은 목록 행의 `⋯` → **`이 공고 복제해서 새로 등록`**.
3. **복제 직후 반드시 덮어쓸 5개**: 제목 / 마감시각(복제되지 않음) / 시작일(비워져 있음) / 모집 인원 / 급여·AI 응대 근거.
   - 복제는 급여·집결지·정책·참고정보를 **그대로 가져온다.** 옆 라인 값이 남으면 AI가 그 값으로 **확답**한다(문자는 되돌릴 수 없다).
4. **하루에 7개를 몰아 올리지 않는다** — 2~3개씩 나누면 한 사람 링크에 뜨는 카드 수가 줄어 오클릭·선점 문제가 완화된다.
5. **마감 정리와 신규 게시는 하루 이상 띄운다** — 같은 날 하면 24시간 교차 가드로 "5명 발송 예정 → 0명 발송"이 된다(정상 동작, 실패 아님).
6. 등록 직후 뜨는 **'대기자에게 안내' 모달은 7개 중 1개에서만 발송**, 나머지는 건너뛰기. **전체 노출일 때는** 문자의 맞춤 공고 링크가 7개를 다 보여주므로 1통으로 충분하다. 지정 노출로 좁힐 계획이면 **좁힌 뒤에** 행 메뉴 `대기자에게 안내`로 보내는 편이 낫다(§3-1).

## 3. 발송 순서 `[사람]`

0. **먼저 7개의 노출 방식을 확인한다.** 하나라도 `지정 노출`이면 아래 2번의 '단 1통'을 그대로 쓰면 안 된다 — §1-2 ⚠️의 (A)/(B) 중 하나를 고른 뒤 진행한다.
1. 먼저 **공고탭 '대기자에게 안내'** 로 약속자에게 1통(현재 대상 5명).
   - **노출을 좁힌 뒤에 안내하면** 명단 밖 약속자는 대상에서 빠진다. `[코드]` 모달이 `명단 밖 N명이 빠졌어요 — 그중 M명은 '먼저 안내드릴게요' 약속`이라고 알려준다. 그 수를 보고 명단을 넓힐지 판단한다.
   - **반대로 안내를 먼저 보내고 나중에 좁히면**, 그 문자를 받은 분은 `[코드]` 자동으로 명단에 남는다(공고명이 적힌 문자를 받은 분이 링크에서 그 공고를 못 보는 일 방지).
2. 그다음 **파이프라인 '문자 보내기'로 인력풀 고지** — 자유 본문 + `#{맞춤링크}`. 전체 노출이면 **1통으로 충분하다**(링크가 전부 보여준다). 지정 노출로 나눴으면 **명단별 1통**이고, 겹친 분은 24시간 가드가 1통으로 줄인다. 어느 쪽이든 **한 사람에게 공고 수만큼 문자가 가지 않게 한다.**
   - `[코드]` 목적을 안 실은 캠페인도 24시간 교차 가드를 타므로, 실수로 두 번 눌러도 같은 사람에게 두 통이 가지 않는다.
3. 1번에서 보낸 5명은 대상에서 뺀다(같은 날 두 경로가 겹치면 2통).
4. `발송 준비 > 최근 14일 안에 캠페인 문자 보낸 분 제외`를 켠다(기본 꺼져 있다).
5. **이미 확정된 54명**: 운행이 멈춰 대기 중인 분이면 보내도 된다. 지금 근무 중인 분이면 진행 단계에서 `확정인력`을 빼고 보낸다. `[코드]` 모달이 포함 인원 수를 알려준다.
6. 차량이 필요한 라인을 알릴 때는 **차량 조건**을 확인한다(도보·미확인이 기본 포함이고 서버가 막지 않는다).
7. 발송을 시작하면 **탭을 닫지 않는다**(565명이면 20분 안팎). 실패 토스트가 떠도 **10분 안에 재시도하지 않는다**(이미 나간 사람에게 중복 발송).

## 4. 매일 관제 순서 `[사람]`

1. **관심 표시 큐** — 공고별로 훑는다. **여기가 핵심**: 한 사람이 여러 공고에 관심을 눌러도 자동 응대는 **첫 1건만** 나간다. 나머지는 문자가 안 나갔으니 수동으로 연락한다.
2. **Slack 알림** — `자동 편입을 건너뜀`·`같은 분의 추가 관심 N건은 큐에 남겨뒀어요` 줄이 보이면 그 사람은 수동 처리 대상이다. `[코드]` 이 알림은 이번에 추가했다(예전엔 전건 스킵이면 알림이 아예 없었다).
3. **사람 확인 필요(paused) 큐** — 한 사람이 여러 카드로 뜰 수 있다. 카드에 공고명이 없어 열어서 확인한다.
4. **확정 대기 큐** → 확정.
5. **충원율 게이지** — `[코드]` 이제 '그 공고에 확정된 사람'만 센다. 그래도 `충원 완료 — 마감하기`를 누르기 전에 실제 확정 인원을 한 번 확인한다(마감하면 미선발 관심자에게 안내 문자가 나간다).

## 5. 절대 하지 않는 것 `[사람]` — 코드가 막지 않는다

- [ ] **확정은 지원자 상세의 확정 모달로만.** 파이프라인 일괄 상태 변경으로 '확정인력'을 만들지 않는다 — 다른 라인 집결지·시작일로 만남장소 문자가 조립되고, 확정된 사람에게 AI가 계속 스크리닝 질문을 보낸다.
- [ ] **만남장소·첫날 안내를 보내기 전 미리보기의 일시·집결지·라인 종류를 눈으로 읽는다**(도시락 라인에 비마트 배차 문구가 나갈 수 있다).
- [ ] **매니저 답장은 실시간 응대에서 공고를 선택한 상태로** 한다.
- [ ] **'지원자 화면' 미리보기에서 관심 버튼을 누르지 않는다**(테스트 계정이 후보로 붙는다).
- [ ] **수동 마감(상태만 마감)보다 마감시각 경과를 쓴다** — 수동 마감은 지원자 링크에서 카드를 즉시 사라지게 해 접수 흔적이 없어진다.

## 6. 발사 직전 전역 확인 `[사람]`

- [ ] **AI 모드** — 지금 `자동`이다. **첫날은 `코파일럿(초안만)`으로 두고** 반응량을 본 뒤 자동으로 올린다. 자동이면 관심 클릭마다 즉시 실문자가 나간다.
- [ ] **에이전트 두뇌 지식이 7개 라인 전부에 사실인지** 확인(익월 5일 지급·과태료 회사 부담 같은 단정 문구가 전 라인 공통으로 주입된다). 다른 라인은 그 공고의 'AI 응대 근거'로 덮는다.
- [ ] 연락처 없는 확정인력 1명(안현진) 연락처 확인.
- [ ] 전화번호 중복 2쌍 정리(같은 사람이 두 행으로 있으면 관심·확정 이력이 갈린다).

---

## 이번에 코드로 막은 것 `[코드]`

| 사고 | 조치 |
|---|---|
| 한 사람이 공고 수만큼 문자를 받음 | 목적 미지정 캠페인도 24시간 교차 가드 적용 |
| 같은 번호로 두 통 | 발송 루프에서 번호 단위 중복 제거 |
| 발송 중 요청이 끊겨 '일부만 나갔는데 실패' | `maxDuration` 300 선언 |
| 미충원 공고에 '충원 완료' 오탐 → 마감·사과 문자 | 충원율·AI 충원 판정을 '그 공고에 확정된 사람'만 세도록 통일 |
| 캠페인 답장을 엉뚱한 공고로 추측 편입 | 활성 공고 2개 이상이면 자동 편입 포기 + Slack 통보 |
| 야간 관심 클릭이 임의 1건만 발송되고 나머지 소멸 | 먼저 누른 것부터 · 나머지는 큐에 유지 · 스킵도 Slack 통보 |
| 지난 시작일이 만남장소 문자로 발송 | 복제 시 시작일 비움 + 과거 시작일 등록 차단 |
| 시니어 오클릭(1탭 즉시 접수·읽는 중 재정렬) | 2단계 확인 · 목록 순서 고정 · 버튼 확대·간격 · 전송 중 전체 비활성 |
| 7장 중 어디가 내 동네인지 모름 | 지점이 없어도 출발지·거리 표시 |
| 노출 명단만 저장되고 공고는 전원에게 그대로 보임 | 명단 저장과 `지정 노출` 전환을 한 번에(원클릭) |
| 전체 노출 공고에 남아 있던 예전 규칙이 전환과 함께 되살아나 고르지 않은 인원에게도 보임 | 규칙이 있으면 2택을 **반드시** 고르게(화면·서버 양쪽 가드) |
| 전환·규칙 삭제로 이야기 중인 공고가 지원자 화면에서 사라짐 | 연결된 인원(관심·후보 + 그 공고 안내 문자 수신자)을 자동으로 명단에 남김 — 파이프라인·수정 모달 두 경로 공통, 전환 뒤 추가한 후보도 포함 |
| 급여만 고치려 저장했는데 방금 좁힌 노출이 되돌아가고 지운 규칙이 부활 | 노출을 만지지 않은 저장은 노출을 전송하지 않음 + 만졌으면 그 사이 바뀐 사실을 알려 덮어쓸지 확인 |
| '규칙 두고 추가'인데 화면이 '이 명단에게만'이라고 단언 | 규칙을 두는 선택이면 제목·버튼·확인 문구가 '규칙 해당 인원 + 이 명단'으로 바뀜 |
| 확인 창에 안 뜬 규칙까지 되돌릴 수 없이 삭제 | 화면이 본 공고 스냅샷을 서버가 대조해 불일치면 409로 막고 현황을 다시 읽게 함 |
| 직전에 쓴 `노출 제외` 모드가 남아 고른 명단이 통째로 제외됨 | 모달을 열 때 항상 '추가' 모드로 초기화 |
| 맞춤 링크에 애초에 안 뜨는 '새로 모집' 공고를 지정 노출로 전환 | 서버가 전환 차단 + 모달에 `새로 모집 — 링크 미노출` 배지 |
| 명단 밖 사람의 답장이 AI·Slack 양쪽에서 조용히 사라짐 | `열린 공고 N개가 모두 이분의 노출 대상 밖` Slack 알림 추가 |
| 명단 화면에서 '이야기 중인 분'을 수동 추가와 구분 못 해 클릭 한 번에 보호 해제 | `이 공고로 이야기 중` 배지 + 제외 시 확인 한 단계 |

## 남는 위험 (사람이 메꿔야 함)

1. **한 사람이 여러 공고에 관심을 눌러도 첫 건만 자동 응대된다.** 화면은 7장 모두 "접수됐어요"라고 하므로, 관심 큐에서 미발송 건을 매일 직접 처리해야 한다.
2. **어느 공고 얘기인지 AI가 틀릴 수 있다.** 인입 문자를 '직전에 보낸 문자의 공고'로 앵커링하고, 캠페인 문자에는 그 값이 없다. 답장은 미답 큐에서 사람이 확인한다.
3. **복제 후 급여·AI 응대 근거 덮어쓰기를 한 번 빠뜨리면** 옆 라인 단가가 확답으로 발송된다(되돌릴 수 없다). 코드가 막지 않는다.
