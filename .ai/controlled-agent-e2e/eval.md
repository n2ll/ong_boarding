# eval
- 제한 세션 대상 허용 1건 red → green. 만료/미래/30분 초과/타 대상/과거 메시지/환경 중지/캐시 공유 차단 확인.
- 관리자 표시 1건 red → green. 모델 중 중단 시 상태 쓰기 1건 red → green. router의 모든 mode 조회에 지원자/수신시각 전달 확인.
- 기존 상담·원문/수신거부·모드·실제 router 런타임 회귀 137/137 통과. 관리자 API 테스트 7/7 통과(단일 테스트 계정, 일반/중복/수신거부/없는 대상 차단, 환경 중지, 세션 해제).
- npm run build exit 0. 후속 테스트 추가 뒤 tsc --noEmit exit 0. 변경 런타임 파일 eslint 및 git diff --check 통과.
- 테스트가 소스 문자열로만 getAgentMode(supabase)를 찾던 부분은 새 인수 형태로 맞췄다. 실제 명시적 수신거부 런타임 테스트도 유지한다.
- DB migration 없음. 기존 예약 제어 행에 기간/대상 제한 JSON을 저장한다. 이전 코드에서도 이 값은 off로 판정된다.
- 운영 배포·실제 자동 SMS 왕복은 다음 단계. 법인폰 대기 1건은 사용자 요청으로 보류하며 삭제·재발송하지 않는다.
- PR #132 (cb80b04bc41fd598d5751087dc1196a245e9241a) Preview deployment 6291856972 성공. 관리자 로그인 → 두뇌 → 응답 모드·안전에서 전화번호 입력, 20분·단일 대상 확인창, 취소 후 완전 중지 유지까지 브라우저 검수했다. 검수 시작은 확정하지 않았다.
- 운영 머지 시도는 자동 승인 검토가 거절했다: 실제 자동 문자 경로를 바꾸는 PR #132 운영 머지에 대한 명시적 승인이 필요하다는 이유. 우회하지 않았으며 운영 코드/DB/자동 응대 모드를 변경하지 않았다. 실제 SMS 발송도 하지 않았다.
- 이후 명시적 승인으로 PR #132 머지 완료: 163725f39988d9e65018162afa25d64f666b4c94. Production deployment 6293524978 성공(2026-09-06 22:31 KST).
- 본인 테스트 계정에만 보이는 가상 공고 2개와 단일 20분 세션을 사용했다. 안내 문자 1회 SOLAPI COMPLETE/4000 확인. 새 수신의 webhook 처리 및 모델 호출까지 확인했다.
- 실제 자동 응대는 `복수 공고 상담 검증 실패: 수신 문자에 없는 원문`으로 중단됐다. 자동 SMS 0건, 신규 상담 발언 0건, 단계 진행·가용성 변경 없음. 운영 raw tool 출력은 남아 있지 않아 실제 잘못된 인용문 자체는 확인하지 못했다.
- 검수 세션 OFF, 가상 공고 2개 마감, 검수 후보 보류·종료, 기존 current_job/focus 복원. 원본 문자·실패 이력 보존. 법인폰 보류 건은 미조치.
- 실제 문맥의 Claude 재전송은 개인정보 전송에 대한 자동 승인 검토 거절로 실행하지 않았다. 대신 운영 데이터/DB 프롬프트를 전송하지 않는 가상 평가에서 동일 실패 재현: 새 질문의 source ID에 과거 관심·가능 시간 문장을 붙여 반환. 추가 모델 사용량은 improve로 적재, 문자/지원자 쓰기 없음.
- 관리자 UI 후속 검수 항목: 제한 세션 중 상단/대화창의 전역 OFF 문구 혼재, 세션 종료창의 전역 중단 문구, 좁은 브라우저에서 문자 발송 버튼 클릭이 하단 설정 이동으로 이어진 현상(키보드 발송 성공; 레이아웃 원인 미확정).
- 현재 전체 실제 왕복 검수는 미완료. 다음: 원문 경계 수정 평가 → 수정 PR/배포 → 새로운 실제 문자로 재검수.
- 수정 평가: 네 단계의 프롬프트 경계 테스트 4건 red(과거 발언보다 앞에 신규 묶음 배치) → green. 원문 불일치 시 발송/기록 차단 유지. 전체 agent 테스트 `node --experimental-strip-types --test lib/agent/*.test.ts lib/agent/stages/consultation.test.ts` 211/211 통과. `npm run build`, 변경 런타임 eslint, `git diff --check` 통과. 빌드에는 기존 job-audience-preview.test.ts의 module 변수 경고 2건만 남았다.
- Sonnet 4.6 가상 모델 평가: 수정 전 순수 조건 질문에 과거 관심/가능 시간 2건을 현재 원문 ID로 반환해 실패. 수정 후 동일 질문은 두 공고의 시간·급여, observations=[], stay로 통과. 별도 연속 수신 2건은 각 source ID에 관심/가능 시간 1건씩, 과거 발언 제외, stay로 통과. 호출당 API 1회, 평가 총 3회, 운영 DB 프롬프트/개인정보 전송 없음, 사용량 improve 적재. 실운영 모델 출력의 직접 재현이 아닌 가상 사례 전후 비교라는 한계가 있다.
- 22:48 KST 최종 상태 대조: OFF, 가상 공고 2개 마감, 검수 후보 종료, 기존 후보 3개 동일, 지원자 status/availability/current_job/focus 원복, 테스트 답장 이후 outbound 0건.
