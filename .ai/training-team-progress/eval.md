# 검증 기록

- 실데이터 읽기(2026-09-10 오전): 응답자 4명 중 현 라인/날짜 희망 3명, 다른 지역 문의 1명. 명확한 선탑 참여 의사는 아직 확인되지 않음. 개인 식별정보는 저장소에 기록하지 않음.
- 선탑 프롬프트 기존 행동 회귀: 115/115. 실제 모델 출력은 별도 검증 전이므로 품질 보장으로 해석하지 않음.
- 진행 패널/API·빌드·브라우저 검수 완료. 운영 배포 결과는 PR 및 비공개 운영 인계 기록으로 확인한다.

- 운영 공고 50/51/52: 선탑 시작 참고범위·실제 교육라인 첫상차지/프로 접선·열린 가능일 질문 반영, CAS+readback 확인. 금액/지급 조건·파일럿 모드/만료/대상·보호 후보 중지 유지.
- 실제 UI: 공고목록의 사람확인150건은 대기149+수동1 잘못합산. 대기제외와 제한자동응대 배너 보완. 별도 지원자 운영 큐에는 의도적 중지/검수 종료도 현안과 섞이고 선택지는 일반적인 재개/직접답변 중심임. 다음 개선은 이 큐에서 실제 운영 예외와 유지할 중지를 구분하고 사유별 결정을 연결하는 것.
- Haiku 가상 2회(문자0/지원자변경0, 사용량 기록): 열린 날짜/시간 질문·교육 후 본인 선택은 수행했으나, 첫 응답에 셋째 주를 백업날짜로 오환산하는 오류 발견. 초기 스크립트의 PASS는 불충분한 날짜 assertion 때문에 인정하지 않음. 재현 기반 출력검증 보완 중.

- 관리자 집중 회귀 최종 53/53. 독립 리뷰에서 JSONB 작성자 객체 키 순서 때문에 재시도가 409가 되는 문제를 발견해 값별 비교로 수정; 해당 회귀 RED→GREEN.
- 최적화 빌드·타입·린트 통과. 로컬 인증 fixture와 API mock으로 PC1280px/모바일390px 새 선탑 상태·팀 이력·동시수정 초안 보존 E2E 2/2 통과, 스크린샷 검토. 실제 DB/SMS 쓰기 없음.
- 날짜 근거 검증: 일반 라인 exploration/screening 자유응답의 근거 없는 선탑 날짜 질문을 열린 질문으로 대체. 복수 상담은 등록 사실을 서버에서 구성하는 기존 경로 유지. 관련 회귀 157/157.
- 추가 Haiku 1회는 날짜·소요시간 문구 정상. 그러나 '(의사 명확함 - 날짜·시간대 대기중)'을 가능시간으로 반환해 수집값 assertion 실패; 전체 PASS로 처리하지 않는다. 미정/대기 수집값 배제와 기존 실제 가능시간 보존 보완 후 결정적 회귀로 확인한다. 모델 호출은 여기서 종료(이번 작업 총3회, 모두 가상 지원자/발송0).
- 수집값 보완: 미정/대기만 있는 값은 병합·완료 판정에서 제외, 기존 실제 가능시간은 보존. 날짜는 미정이나 화요일 오전이 가능하다는 부분 응답도 삭제하지 않도록 회귀로 확인.
- 마지막 수집값 변경 후 general-line/실제 stage 경로 회귀 50/50 및 최종 빌드·타입·린트 exit0. 기존 무관한 테스트 파일의 ESLint 경고 2건은 변경하지 않음.
- 구현 후 필수 후속: 운영 예외 큐에서 의도적 중지·검수 종료와 실제 미해결을 구분하고, 사유별 해결안/결정 버튼을 제공한다. 이번 PR은 그 전체 흐름의 완성으로 간주하지 않는다.
