# 좁은 화면 문자 입력·발송 검수 — 2026-09-07
- 목적: 발송 버튼 클릭이 하단 설정 이동으로 이어졌던 관찰을 재검수한다. 실발송 없이 로컬 인증·API fixture 사용, 외부 요청 차단.
- 원래 겹침 현상은 375×667, 390×844, 768×900, 1280×900에서 재현되지 않았다. 4개 크기 모두 실제 좌표 클릭 1회 → 올바른 지원자·공고로 모의 발송 1회, /live 유지.
- 별도로 375px/390px에서 입력창 폭이 109.52px/124.52px로 줄어 문자 수·단축키 안내가 깨지는 현상 확인. 최소 가독 폭 검증은 수정 전 2개 실패, 나머지 2개 통과.
- 변경: 작은 화면에서는 입력창이 한 줄 전체를 차지하고 발송 버튼들은 다음 줄에 배치. sm 이상은 기존 가로 배치 유지.
- 검증: `npx --no-install playwright test --config=playwright.consultation.config.ts` — 빌드·타입·린트 통과 후 브라우저 12/12 통과. 신규 4개 크기 중 390px는 터치, 나머지는 좌표 클릭 검증. 기존 AI 표시·복수 공고 원문 표시 포함.
- 수정 전후 화면은 `/tmp/sms-composer-375-before.png`, `/tmp/sms-composer-375.png`; 나머지 크기도 `/tmp/sms-composer-{width}.png`. 직접 이미지 확인 완료.
- 한계: 실제 모바일 OS 키보드·특정 과거 배포·이전 자동화 클릭 좌표의 문제를 해결했다고 주장하지 않는다.
