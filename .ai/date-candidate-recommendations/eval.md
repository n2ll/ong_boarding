# 검증
- 관련 단위 검수: 날짜 추천 9건 포함 추천·날짜 보드·진행 기록 21/21 통과.
- 독립 리뷰: 확정 기록에 다른 라인 중복과 진행 중단이 동시에 있으면 두 이유가 모두 표시되도록 수정. 실패 재현 후 통과.
- 변경 3개 소스 ESLint 및 `git diff --check` 통과.
- 실제 컴포넌트 + 가상 7명으로 CUA 검수: 9/21 우선 후보 → 9/22 해당일 후보로 순서 변경, 전체 더 보기, 보류 후보 이름 검색, 메모 입력창 연결 확인.
- 390×844 / 1280×900에서 가로 넘침 없음. 필터 기본 접힘과 추천 이유·연락 버튼 확인.
- 가상 화면 쓰기 요청 0건. 운영 DB·지원자 기록·문자·모델 호출 없음. 검수 페이지는 빌드 전 삭제.
- 날짜 미선택·과거 날짜는 컴포넌트의 기존 순서 유지 분기를 코드로 확인. 브라우저에서는 미래 두 날짜 전환을 검수.
- `npm run build` 통과. 기존 비차단 lint 경고만 남음.
