# 검증
- 단위 검수 34/34: 운행 구역 13건 + 초안 직렬화/복원/저장/생성 맥락. 구역 교체 후 실제 `collectionFactFromBody`가 수정된 수거 조건을 읽는 연동 포함.
- 가상 본문과 실제 편집 컴포넌트로 CUA 검수: 간단 편집→본문, 전체 편집→간단 편집, Enter 줄바꿈, 급여 보존, 문자 별도 편집, 기존/중복 제목 폴백, 구역 밖 중복 조건 폴백, 원문 비교 확인.
- 전체 편집 도중 구역 식별이 바뀌면 입력이 재생성되던 문제를 발견했다. 같은 details/textarea를 유지하고 편집 중 펼침 상태를 보존한 뒤 왕복 입력·포커스·전체 본문 표시 유지 확인.
- 390×844 / 1280×900 레이아웃 확인. 가로 넘침 없음. 임시 검수 페이지 삭제·서버 종료·뷰포트 복원 완료.
- 실제 Sonnet 공고 생성 가상 평가: 초기 2건에서 주소 포함 반납 기한이 상담 인용에서 탈락함을 발견. 도구 스키마 설명에 줄 분리 예시를 추가했다. 중간 3건에서 문자 시각의 오전/오후 변환 및 배송→출발 오표현을 발견해 24시간 표기·원문 동작 보존을 명시했다.
- 최종 3건 모두 핵심 기준 통과: (1) 일부 배송지 맞수거, 가방 기준일 미추정, 당일 반납 보존 (2) 오후 재방문/당일 가방/17:00 반납, 오전 별도 라인 혼합 없음 (3) 배송만 명시하면 수거 없음으로 단정하지 않고 미기재로 해석. 최종 문자 시각과 배송 순서도 원문 대조.
- 모델 호출 합계 8회, 모두 기존 `job_generate` 사용량 기록 경로 사용. 최종 3건은 입력 10,614 / 출력 3,103 토큰. 실제 공고·지원자·문자·배차 쓰기 없음.
- 간단 편집 허용은 보수적이다. 직무명·근무지에 수거/상차 등이 반복된 최종 모델 초안 3건도 전체 편집으로 표시된다. 구조화된 운행 단락과 생성 원문 비교는 유지한다. 생성 결과는 초안이며 자동 사실 검증 완료를 의미하지 않는다.
- 재현 자료: `/tmp/ong-job-operations-model-eval.json`, `/tmp/ong-job-operations-tests.log`, `/tmp/ong-job-operations-build.log` (로컬).

- 독립 리뷰 P2 2건 조치: 구역 밖 한국어 시각(오전 7시 등)도 전체 편집 대상으로 추가(RED 1건 → GREEN), 근무조건 배치 제약은 배송·수거 공고에만 한정해 비배송 근무시간 보존. 변경 소스 ESLint 및 빌드 타입 검사에 포함.
- 최종 `npm run build` 통과. 기존 비차단 lint 경고만 남음.
